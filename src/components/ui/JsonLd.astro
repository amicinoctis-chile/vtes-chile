---
interface Props {
  data: Record<string, unknown>;
}

const { data } = Astro.props;
const { cspNonce } = Astro.locals;
---

<script type="application/ld+json" nonce={cspNonce} set:html={JSON.stringify(data)} />
